# ECS Service Discovery をシミュレートする Docker Compose 設定
# 
# 本番環境と同じ構成でテスト可能:
#   - バックエンドを外部に公開しない（Service Discovery経由のみ）
#   - フロントエンドからのみアクセス可能
#   - ブラウザからバックエンドへの直接アクセス不可
#
# 使用方法:
#   docker compose -f docker-compose.service-discovery.yml up --build
#

services:
  db:
    image: postgres:16.2
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: epimetric_patient_dev
      TZ: Asia/Tokyo
    # 開発時のDB接続確認用にポート公開（本番では不要）
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_sd:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      RAILS_ENV: development
      DATABASE_HOST: db
      DATABASE_NAME: epimetric_patient_dev
      DATABASE_USERNAME: postgres
      DATABASE_PASSWORD: password
      DATABASE_PORT: 5432
      RAILS_MAX_THREADS: 5
      TZ: Asia/Tokyo
      # CORS設定: フロントエンドからのアクセスを許可
      CORS_ALLOWED_ORIGINS: "http://localhost:4000"
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails db:create db:migrate && bundle exec rails s -b '0.0.0.0'"
    volumes:
      - ./backend:/app
      - bundle_cache_sd:/usr/local/bundle
    depends_on:
      db:
        condition: service_healthy
    # ⚠️ ポート公開を削除: 外部から直接アクセス不可（Service Discovery風）
    # ports:
    #   - "3000:3000"
    expose:
      - "3000"  # コンテナ間通信のみ許可
    networks:
      - app-network
    tty: true
    stdin_open: true

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      NODE_ENV: development
      # ⚠️ CSR用のAPI URLもフロントエンド経由に変更
      # ブラウザからのAPI呼び出しは Next.js API Routes を経由
      NEXT_PUBLIC_API_URL: http://localhost:4000/api
      # SSR用: Service Discovery経由（コンテナ間通信）
      API_URL: http://backend:3000
      # バックエンドの実際のURL（Next.js API Routes内で使用）
      BACKEND_API_URL: http://backend:3000
      TZ: Asia/Tokyo
    volumes:
      - ./frontend:/app
      - front_node_modules_sd:/app/node_modules
    command: yarn dev -p 4000
    ports:
      - "4000:4000"  # フロントエンドのみ外部公開
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  postgres_data_sd:
  front_node_modules_sd:
  bundle_cache_sd:

networks:
  app-network:
    driver: bridge

